
<h1>Home</h1>
<ul>
    <% @flights.each do |flight| %>
        <li>
            <%= flight.route %>: $<%= flight.price %>
            <button id= <%= flight.id%>>Book Me</button>
        </li>
    <% end %>
</ul>
